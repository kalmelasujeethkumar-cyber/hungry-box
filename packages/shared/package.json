{
  "name": "@hungrybox/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types/contracts for Hungry Box (frontend + backend). Type-only consumption in Phase 1 via built declaration files (dist).",
  "type": "module",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
